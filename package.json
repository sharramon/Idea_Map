{
  "name": "idea-map",
  "version": "1.0.0",
  "description": "Personal Idea Mapping Tool — diary text to interactive graph",
  "scripts": {
    "build": "tsc",
    "cli": "ts-node src/cli.ts",
    "view": "ts-node src/cli.ts view",
    "process": "ts-node src/cli.ts process"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "commander": "^12.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
